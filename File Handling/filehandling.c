#include<stdio.h>

/*
int main()
{
	FILE *fp=NULL;//file pointer to point file
	//file is alias of pointer structure
	char ch;
	fp=fopen("d:\\Suraj.txt","w");//w-wite, r-read, a-append, w+ -write&read, r+ -read&write,a+ - append&write
	if(fp!=NULL)
	{
		ch=getche();
		while(ch!='@')
		{
			fputc(ch,fp);
			ch=getche();
		}
		
	printf("FIle is created successfully");
	}
	else
	printf("file is not created successfully");
	fclose(fp);
	return 0;
}*/
/*
int main()
{

FILE *fp=NULL;//file pointer to point file
	//file is alias of pointer structure
	char ch;int i=0;
	char a[100];
	fp=fopen("d:\\Suraj.txt","r");//w-wite, r-read, a-append, w+ -write&read, r+ -read&write,a+ - append&write
	if(fp==NULL)
	{
		
	printf("FIle does not exist");
	}
	else
	{
		while(!feof(fp))
		{

		a[i]=fgetc(fp);
		ch=a[i];
		printf("%c",ch);
		i++;
	}
	printf("file exist");
}

printf("\n%s",a);
	fclose(fp);
	return 0;
}
*/
int main()
{
	
	
}






















